@page
@using Web
@model Web.Pages.IndexModel

<!DOCTYPE html>
<html>
<head>
    <title>Tasks</title>
    <link rel="stylesheet" href="/css/index.css" />
</head>
<body>
    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="error">@Model.ErrorMessage</div>
    }

    <div class="container-card">
        <header class="page-header">
            <div>
                <h1>Tasks</h1>
            </div>
            
        </header>

        <div class="add-form">
            <form method="post" style="display:flex;flex:1;gap:12px;align-items:center">
                @Html.AntiForgeryToken()
                <input class="input" type="text" name="title" placeholder="Add a new task..." required autofocus />
            </form>
        </div>

        <h2 style="font-size:13px;color:var(--muted);text-transform:uppercase;letter-spacing:0.5px;margin:8px 0 10px 0">Pending (@Model.PendingTodos.Length)</h2>
        @if (Model.PendingTodos.Any())
        {
            <div class="list">
                @foreach (var todo in Model.PendingTodos)
                {
                    <div class="item">
                        <div class="left">
                            <div class="avatar">T</div>
                            <div style="min-width:0">
                                @if (Model.EditingId == todo.Id)
                                {
                                    <form method="post" style="display:inline-block">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@todo.Id" />
                                        <input type="text" name="title" value="@todo.Title" required class="edit-input" autofocus />
                                    </form>
                                }
                                else
                                {
                                    <div class="title">@todo.Title</div>
                                    <div class="muted">#@todo.Id</div>
                                }
                            </div>
                        </div>

                        <div class="actions">
                            <form method="post" style="display:inline-flex;gap:8px;align-items:center">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@todo.Id" />
                                <input type="hidden" name="title" value="@todo.Title" />
                                <input type="hidden" name="isCompleted" value="@(todo.IsCompleted.ToString().ToLower())" />
                                @if (Model.EditingId == todo.Id)
                                {
                                    <button type="submit" name="action" value="Update" class="icon-btn primary" title="Save" aria-label="Save">✓</button>
                                    <button type="submit" name="action" value="CancelEdit" class="icon-btn danger" title="Cancel" aria-label="Cancel">✖</button>
                                }
                                else
                                {
                                    <button type="submit" name="action" value="Edit" class="icon-btn warn" title="Edit" aria-label="Edit">✏️</button>
                                    <button type="submit" name="action" value="Toggle" class="icon-btn primary" title="Complete" aria-label="Complete">✓</button>
                                    <button type="submit" name="action" value="Delete" class="icon-btn danger" title="Delete" aria-label="Delete" onclick="return confirm('Delete this task?')">🗑️</button>
                                }
                            </form>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <p><em>No tasks yet. Add one above!</em></p>
        }

        <h2 style="font-size:13px;color:var(--muted);text-transform:uppercase;letter-spacing:0.5px;margin:22px 0 10px 0">Completed (@Model.CompletedTodos.Length)</h2>
        @if (Model.CompletedTodos.Any())
        {
            <div class="list">
                @foreach (var todo in Model.CompletedTodos)
                {
                    <div class="item">
                        <div class="left">
                            <div class="avatar done">✓</div>
                            <div>
                                <div class="title completed">@todo.Title</div>
                                <div class="muted">#@todo.Id</div>
                            </div>
                        </div>
                        <div class="actions">
                            <form method="post" style="display:inline-flex;gap:8px;align-items:center">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@todo.Id" />
                                <input type="hidden" name="title" value="@todo.Title" />
                                <input type="hidden" name="isCompleted" value="@(todo.IsCompleted.ToString().ToLower())" />
                                <button type="submit" name="action" value="Toggle" class="icon-btn warn" title="Reopen" aria-label="Reopen">↺</button>
                                <button type="submit" name="action" value="Delete" class="icon-btn danger" title="Delete" aria-label="Delete" onclick="return confirm('Delete this task?')">🗑️</button>
                            </form>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <p><em>Nothing completed yet.</em></p>
        }
    </div>
</body>
</html>
